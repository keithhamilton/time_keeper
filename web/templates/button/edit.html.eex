<h2>Edit button</h2>

<%= render "form.html", changeset: @changeset,
                        action: button_path(@conn, :update, @button),
                        jobcodes: @jobcodes %>

<%= link "Back", to: button_path(@conn, :index) %>
